# The Liscere passive observer (scripts/liscere_observe.py) runs on the Python standard library
# only (plus the system `tshark` binary for capture). It needs no third-party Python packages.
#
# The optional discovery-map TUI (scripts/liscere_ui.py) is the only component with a dependency:
rich
